[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/whatsapp/Dockerfile"

[deploy]
startCommand = "node /app/apps/whatsapp/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
